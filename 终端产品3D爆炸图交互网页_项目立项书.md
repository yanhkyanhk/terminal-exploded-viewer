# 终端产品 3D 爆炸图交互网页项目立项书

> 项目类型：Web 3D 三维可视化 / 前端工程  
> 项目定位：终端产品三级交互式拆解与爆炸图展示系统  
> 核心链路：Product → Module → Part  
> 技术方向：React + TypeScript + Three.js + React Three Fiber  
> 版本：V1.0  
> 状态：立项 / 可进入开发阶段

---

## 1. 项目概述

### 1.1 项目名称

**终端产品 3D 爆炸图交互网页**

英文可命名为：

`Interactive Product Exploded View`

### 1.2 项目背景

智能手机、智能音箱、可穿戴设备、无人机、POS 终端等硬件产品通常由多个功能模组构成，每个功能模组内部又包含若干零部件。

传统二维结构图或静态爆炸图虽然能够表达部件关系，但在产品展示、教学演示、技术拆解、售后培训等场景中，存在以下问题：

- 无法从任意视角观察产品结构；
- 无法按层级逐步查看内部组件；
- 部件之间的归属关系不够直观；
- 缺少用户交互与动态反馈；
- 难以与 BOM、部件信息、技术说明等业务信息形成联动。

因此，本项目计划构建一个基于浏览器运行的可交互 Web 3D 产品爆炸图系统。

用户可以从完整产品开始，通过点击逐层深入查看：

```text
整机 Product
    ↓
功能模组 Module
    ↓
组成零件 Part
```

并支持爆炸、收合、旋转、缩放、聚焦、高亮、标签及层级导航等交互。

---

## 2. 项目建设目标

### 2.1 总体目标

建设一个具备良好交互体验、清晰场景架构和工程可维护性的 Web 3D 产品拆解系统，实现：

1. 完整产品三维展示；
2. Product → Module → Part 三级结构建模；
3. 整机点击后模组爆炸展开；
4. 模组点击后零件继续爆炸展开；
5. 支持逐级收合与返回；
6. 支持鼠标或触摸旋转、缩放和平移；
7. 支持 Hover 高亮和 Raycasting 拾取；
8. 支持层级切换后的相机自动聚焦；
9. 支持模组 / 零件标签与基础信息展示；
10. 满足桌面端流畅渲染及基本移动端使用要求。

### 2.2 核心技术目标

本项目不仅实现视觉效果，更重点验证以下工程能力：

- Three.js / WebGL 场景搭建能力；
- glTF / GLB 模型加载与组织能力；
- Scene Graph 管理能力；
- Raycasting 3D 拾取能力；
- 层级化领域模型设计能力；
- 前端有限状态机设计能力；
- 3D 动画与状态同步能力；
- 相机控制与自动聚焦能力；
- React UI 与 3D Scene 解耦能力；
- Web 3D 性能优化能力。

---

## 3. 项目范围

## 3.1 本期范围

本期系统固定支持三级结构：

```text
Product
  └── Module
        └── Part
```

建议示例规模：

- Product：1 个；
- Module：5～7 个；
- 每个 Module 下 Part：3～6 个；
- Part 不继续向下展开。

### 3.2 本期不做

V1.0 暂不建设：

- CAD 编辑能力；
- 3D 模型在线编辑器；
- 第四级及更深的结构层级；
- 后台管理系统；
- 用户登录与权限系统；
- 数据库；
- 多人协作；
- 实时协同编辑；
- 物理引擎；
- 工业级 CAD 精度分析；
- 复杂 Shader 系统；
- 高级数字孪生能力。

---

## 4. 用户核心流程

## 4.1 L1：整机视图

页面打开后：

```text
加载页面
   ↓
加载 GLB / glTF
   ↓
初始化灯光、相机、控制器
   ↓
展示完整 Product
   ↓
允许 Rotate / Zoom / Pan
```

此时所有 Module 和 Part 均保持装配状态。

---

## 4.2 L2：模组爆炸视图

用户点击完整产品：

```text
Click Product
   ↓
Raycasting
   ↓
识别 Product
   ↓
状态 PRODUCT → MODULE
   ↓
所有 Module 从产品中心向外分离
   ↓
相机适当拉远 / 聚焦
   ↓
展示 Module 标签
```

示意：

```text
                Camera
                  ↗

Display   ←   Product   →   Battery

                  ↓
               BackCover

               Mainboard
                  ↘
```

---

## 4.3 L3：零件爆炸视图

用户在 L2 点击某一个 Module：

```text
Click Module
   ↓
Raycasting
   ↓
识别 Module
   ↓
状态 MODULE → PART
   ↓
selectedModuleId = 当前模组
   ↓
仅当前 Module 的 Parts 爆炸
   ↓
Camera Focus 当前 Module
   ↓
显示 Part 标签与信息
```

例如：

```text
Camera Module

├── Lens
├── Sensor
├── PCB
├── Housing
└── Connector
```

本期明确规定：

**同一时刻只允许一个 Module 进入 Part 爆炸状态。**

不支持多个 Module 同时展开。

---

## 4.4 逐级回退

回退必须遵循层级：

```text
PART
  ↓ Back
MODULE
  ↓ Back
PRODUCT
```

不能把所有返回动作统一处理成“一键恢复整机”。

具体行为：

### PART → MODULE

- 当前 Module Parts 收合；
- selectedModuleId 清空；
- 所有 Modules 保持 L2 爆炸状态；
- Camera 返回 Module 层视角。

### MODULE → PRODUCT

- 所有 Modules 收合；
- 当前选中状态清空；
- Camera 回到初始视角。

---

## 5. 功能需求

## 5.1 P0 核心功能

P0 为必须完成项。

### 5.1.1 3D 模型加载

支持：

- GLB；
- glTF。

要求：

- 异步加载；
- 显示 Loading 状态；
- 支持加载失败提示；
- 加载完成后自动初始化 Scene Registry。

---

### 5.1.2 三级产品结构

建立清晰的：

```text
Product
Module
Part
```

业务数据结构。

3D 模型层级不得直接代替业务领域模型。

---

### 5.1.3 Raycasting 拾取

支持：

```text
Pointer
   ↓
Raycaster
   ↓
Mesh
   ↓
Logical Node Resolver
   ↓
Product / Module / Part
```

Raycast 命中子 Mesh 后，需要能够向父节点寻找其业务归属。

---

### 5.1.4 Hover 高亮

当前层级可交互对象在鼠标悬停时需要：

- cursor 变化；
- 轮廓高亮；
- emissive / glow 等视觉反馈之一。

不同层级只允许高亮当前层级可操作对象。

---

### 5.1.5 爆炸动画

支持：

- Product → Modules Explode；
- Module → Parts Explode；
- Parts → Module Collapse；
- Modules → Product Collapse。

动画需要：

- 点击后立即响应；
- 平滑过渡；
- 无明显跳变；
- 支持反向收合；
- 不产生位置累积误差。

---

### 5.1.6 OrbitControls

桌面端：

- 左键拖拽：旋转；
- 滚轮：缩放；
- 右键拖拽：平移。

移动端：

- 单指旋转；
- 双指缩放；
- 基本触控适配。

---

### 5.1.7 Camera Focus

层级切换后自动调整：

```text
camera.position
+
OrbitControls.target
```

支持：

- focusProduct；
- focusModules；
- focusModule；
- resetCamera。

---

### 5.1.8 Breadcrumb

显示当前层级：

```text
Product
```

或：

```text
Product / Modules
```

或：

```text
Product / Camera Module / Parts
```

同时支持返回上层。

---

### 5.1.9 标签

L2 展示：

- Module Name。

L3 展示：

- Part Name。

标签可采用：

- Drei Html；
- CSS2DRenderer；
- 其他 Web Overlay 方案。

---

### 5.1.10 Loading / Error

必须包含：

```text
Loading Model...

65%
```

加载失败：

```text
Model failed to load

Retry
```

避免模型尚未就绪时出现空白界面。

---

## 5.2 P1 增强功能

P0 完成后实现：

- 部件详情面板；
- Reset View；
- ESC 快捷返回；
- 加载百分比；
- 动画状态反馈；
- 响应式 UI；
- 简单性能监控；
- 空白区域点击返回上层。

---

## 5.3 P2 加分功能

优先建议：

### P2-1 爆炸程度 Slider

支持：

```text
0% ───────────── 100%
```

用户连续调节爆炸距离。

### P2-2 BOM ↔ 3D 双向联动

```text
点击 BOM Item
     ↓
3D Object Focus + Highlight
```

以及：

```text
点击 3D Object
     ↓
BOM Item Highlight
```

其他可选：

- 外壳透明模式；
- Clipping Plane 剖切；
- 移动端陀螺仪。

---

# 6. 非功能需求

## 6.1 性能

目标：

- 桌面端主流配置稳定 ≥ 50 FPS；
- 动画过程无明显掉帧；
- 模型加载过程中不长时间白屏；
- 无明显内存泄漏；
- 长时间交互后帧率不持续恶化。

---

## 6.2 兼容性

支持最新版：

- Chrome；
- Edge；
- Firefox；
- Safari。

---

## 6.3 响应式

PC：

- 完整功能。

Mobile：

- Rotate；
- Zoom；
- Click；
- Back；
- 基础标签与层级浏览。

复杂 UI 可在小屏适当简化。

---

# 7. 技术选型

## 7.1 前端技术栈

```text
React
TypeScript
Vite
```

职责：

- 页面结构；
- UI；
- 状态绑定；
- 工程构建。

---

## 7.2 三维技术栈

```text
React Three Fiber
Three.js
@react-three/drei
```

职责：

- WebGL Renderer；
- Scene；
- Camera；
- Object3D；
- GLTF；
- OrbitControls；
- Labels。

---

## 7.3 状态管理

```text
Zustand
```

管理：

- currentLevel；
- selectedModuleId；
- selectedPartId；
- hoveredNodeId；
- isAnimating。

不保存：

- THREE.Mesh；
- THREE.Group；
- THREE.Object3D。

---

## 7.4 动画

建议：

```text
GSAP
```

用于：

- Module Explosion；
- Part Explosion；
- Collapse；
- Camera Tween。

---

## 7.5 模型格式

推荐：

```text
GLB
```

原因：

- 单文件；
- 便于部署；
- glTF 标准；
- Three.js 原生支持成熟。

---

# 8. 总体技术架构

系统采用四层设计：

```text
┌──────────────────────────────────────┐
│ Presentation Layer                   │
│                                      │
│ React UI                             │
│ Breadcrumb / Toolbar / InfoPanel     │
├──────────────────────────────────────┤
│ Application Layer                    │
│                                      │
│ Viewer State                         │
│ State Machine                        │
│ Interaction Command                  │
├──────────────────────────────────────┤
│ Domain Layer                         │
│                                      │
│ Product                              │
│ Module                               │
│ Part                                 │
│ Explosion Config                     │
├──────────────────────────────────────┤
│ Rendering Layer                      │
│                                      │
│ R3F / Three.js                       │
│ GLTF / Raycaster / Camera / Animation│
└──────────────────────────────────────┘
```

---

# 9. 核心架构原则

必须保证：

```text
Product Structure
        ≠
Three.js Scene Graph
        ≠
Viewer State
```

三者之间通过 ID 和映射机制关联。

### Domain Model

描述：

> 产品是什么。

### Scene Graph

描述：

> 产品如何被 Three.js 渲染。

### Viewer State

描述：

> 用户现在处于什么交互状态。

---

# 10. 领域模型设计

## 10.1 Node Type

```ts
export type ProductNodeType =
  | 'product'
  | 'module'
  | 'part'
```

---

## 10.2 Vec3

Domain 层避免直接依赖 Three.js：

```ts
export type Vec3 = [
  number,
  number,
  number
]
```

---

## 10.3 ExplosionConfig

```ts
export interface ExplosionConfig {
  direction?: Vec3
  distance?: number
  delay?: number
  enabled?: boolean
}
```

---

## 10.4 PartNode

```ts
export interface PartNode {

  id: string

  type: 'part'

  name: string

  objectName: string

  parentId: string

  explode?: ExplosionConfig

  metadata?: {
    description?: string
    material?: string
    code?: string
  }
}
```

---

## 10.5 ModuleNode

```ts
export interface ModuleNode {

  id: string

  type: 'module'

  name: string

  objectName: string

  parentId: string

  explode?: ExplosionConfig

  parts: PartNode[]

  metadata?: {
    description?: string
  }
}
```

---

## 10.6 ProductNode

```ts
export interface ProductNode {

  id: string

  type: 'product'

  name: string

  objectName: string

  modules: ModuleNode[]

  metadata?: {
    description?: string
    modelUrl?: string
  }
}
```

---

# 11. 产品配置示例

```ts
export const productConfig: ProductNode = {

  id: 'terminal-phone',

  type: 'product',

  name: 'Smart Terminal',

  objectName: 'Product_Root',

  metadata: {
    modelUrl: '/models/terminal.glb'
  },

  modules: [

    {
      id: 'camera-module',

      type: 'module',

      name: 'Camera Module',

      objectName: 'Module_Camera',

      parentId: 'terminal-phone',

      explode: {
        direction: [1, 0.4, 0.2],
        distance: 1.6
      },

      parts: [

        {
          id: 'camera-lens',

          type: 'part',

          name: 'Camera Lens',

          objectName: 'Part_Camera_Lens',

          parentId: 'camera-module',

          explode: {
            direction: [0, 0, 1],
            distance: 0.8
          }
        }

      ]
    }

  ]
}
```

---

# 12. GLB 节点命名规范

推荐：

```text
Scene
└── Product_Root

    ├── Module_Display
    │   ├── Part_Display_Glass
    │   ├── Part_Display_Panel
    │   └── Part_Display_Frame

    ├── Module_Battery
    │   ├── Part_Battery_Cell
    │   └── Part_Battery_Connector

    ├── Module_Mainboard
    │   ├── Part_Mainboard_PCB
    │   ├── Part_Mainboard_CPU
    │   └── Part_Mainboard_Shield

    └── Module_Camera
        ├── Part_Camera_Lens
        ├── Part_Camera_Sensor
        └── Part_Camera_Housing
```

避免：

```text
Cube001
Cube002
mesh27
Object19
```

GLB 节点名称必须具备语义。

---

# 13. Scene Graph Mapping

## 13.1 设计目标

建立：

```text
Domain Node ID
       ↕
Three.js Object3D
```

双向映射。

---

## 13.2 SceneRegistry

```ts
export class SceneRegistry {

  private nodeToObject =
    new Map<string, THREE.Object3D>()

  private objectToNode =
    new Map<string, string>()

}
```

核心接口：

```ts
register(nodeId, object)

getObject(nodeId)

getNodeId(object)

getRuntimeNode(nodeId)
```

---

## 13.3 RuntimeSceneNode

```ts
export interface RuntimeSceneNode {

  nodeId: string

  object: THREE.Object3D

  type:
    | 'product'
    | 'module'
    | 'part'

  initialTransform: {
    position: THREE.Vector3
    rotation: THREE.Euler
    scale: THREE.Vector3
  }

  explodedPosition?: THREE.Vector3
}
```

---

# 14. Scene 注册流程

```text
GLB Loaded
   ↓
读取 ProductConfig
   ↓
找到 Product_Root
   ↓
register(Product)
   ↓
遍历 Modules
   ↓
scene.getObjectByName()
   ↓
register(Module)
   ↓
遍历 Parts
   ↓
register(Part)
```

如果配置中的 objectName 无法在 GLB 中找到：

- 控制台输出明确错误；
- 阻止该节点进入交互系统；
- 开发模式给出配置校验提示。

---

# 15. Node Resolver

Raycaster 很可能命中：

```text
Mesh_Frame
```

而业务注册的是：

```text
Module_Camera
```

因此需要向父节点递归查找：

```ts
function resolveNodeId(
  object: THREE.Object3D
): string | null {

  let current: THREE.Object3D | null = object

  while (current) {

    const nodeId =
      registry.getNodeId(current)

    if (nodeId) {
      return nodeId
    }

    current = current.parent
  }

  return null
}
```

最终：

```text
Mesh_Frame
    ↓
Module_Camera
    ↓
camera-module
```

---

# 16. Viewer 状态机设计

## 16.1 Level

```ts
export type ViewerLevel =
  | 'PRODUCT'
  | 'MODULE'
  | 'PART'
```

---

## 16.2 ViewerState

```ts
export interface ViewerState {

  level: ViewerLevel

  selectedModuleId:
    string | null

  selectedPartId:
    string | null

  hoveredNodeId:
    string | null

  isAnimating:
    boolean
}
```

---

## 16.3 初始状态

```ts
const initialState = {

  level: 'PRODUCT',

  selectedModuleId: null,

  selectedPartId: null,

  hoveredNodeId: null,

  isAnimating: false
}
```

---

# 17. 状态机事件

```ts
type ViewerEvent =

  | {
      type: 'EXPLODE_PRODUCT'
    }

  | {
      type: 'ENTER_MODULE'
      moduleId: string
    }

  | {
      type: 'SELECT_PART'
      partId: string
    }

  | {
      type: 'BACK'
    }

  | {
      type: 'RESET'
    }
```

---

# 18. 状态转移表

| 当前状态 | Event | 下一状态 | 行为 |
|---|---|---|---|
| PRODUCT | EXPLODE_PRODUCT | MODULE | 所有 Module 爆炸 |
| MODULE | ENTER_MODULE | PART | 指定 Module Parts 爆炸 |
| PART | SELECT_PART | PART | 选择 Part |
| PART | BACK | MODULE | Parts 收合 |
| MODULE | BACK | PRODUCT | Modules 收合 |
| 任意 | RESET | PRODUCT | 全部复位 |

---

# 19. 动画并发控制

动画运行期间必须防止重复交互：

```ts
if (state.isAnimating) {
  return
}
```

目标：

避免：

```text
PRODUCT → MODULE
```

尚未结束时，又执行：

```text
MODULE → PART
```

造成 Transform 状态冲突。

---

# 20. Interaction Guard

不同 Level 只允许操作对应业务节点。

### PRODUCT

允许：

```text
Product
```

### MODULE

允许：

```text
Module
```

### PART

允许：

```text
Part
```

示例：

```ts
canEnterModule =
  state.level === 'MODULE'
  &&
  node.type === 'module'
  &&
  !state.isAnimating
```

---

# 21. 交互架构

禁止：

```ts
onClick={() => {
  mesh.position.x += 5
}}
```

必须：

```text
Pointer Event
     ↓
InteractionManager
     ↓
Raycasting
     ↓
NodeResolver
     ↓
Viewer State Machine
     ↓
ExplosionManager
```

UI 或 Pointer Event 不直接修改 Mesh。

---

# 22. Explosion Engine

## 22.1 基础模型

每个节点保存：

```text
assembledPosition
explodedPosition
```

公式：

```text
explodedPosition
=
assembledPosition
+
direction × distance
```

---

## 22.2 爆炸方向优先级

采用三级策略。

### Level 1：人工配置

优先使用：

```ts
explode: {
  direction: [1, 0.2, 0],
  distance: 2
}
```

优点：

- 可控；
- 视觉布局稳定；
- 适合不同产品的人工优化。

---

### Level 2：自动方向计算

没有 direction 时：

```text
ModuleCenter
-
ProductCenter
```

得到：

```text
Product Center
      ↓
      ●
           ↗ Module
```

实现：

```ts
direction
  .subVectors(
    moduleCenter,
    productCenter
  )
  .normalize()
```

Part 同理：

```text
PartCenter
-
ModuleCenter
```

---

### Level 3：默认方向兜底

若方向计算得到零向量，则按照固定轴向分配：

```text
Part 1 → +X
Part 2 → -X
Part 3 → +Y
Part 4 → -Y
Part 5 → +Z
Part 6 → -Z
```

防止多个零件完全重叠。

---

# 23. Bounding Box Center

不能直接把：

```ts
object.position
```

当作视觉中心。

统一使用：

```ts
const box =
  new THREE.Box3()
    .setFromObject(object)

const center =
  box.getCenter(
    new THREE.Vector3()
  )
```

进行 Explosion Direction 与 Camera Focus 计算。

---

# 24. World / Local Transform

Box3 Center 通常按 World Space 计算，而：

```ts
object.position
```

属于 Parent Local Space。

因此必须统一进行坐标转换。

例如：

```ts
const localTarget =
  object.parent!
    .worldToLocal(
      worldTarget.clone()
    )
```

禁止直接混合 World Position 和 Local Position。

---

# 25. Explosion Distance

建议默认范围：

### Module

```text
1.5 ～ 2.5
```

### Part

```text
0.5 ～ 1.2
```

也可根据 Parent Bounding Box 动态计算：

```ts
distance =
  parentSize.length()
  * factor
```

参考：

```text
Module factor ≈ 0.25

Part factor ≈ 0.15
```

实际值根据产品视觉效果调整。

---

# 26. Explosion Progress

从第一版就支持：

```text
progress: 0 → 1
```

计算：

```text
currentPosition
=
lerp(
  assembledPosition,
  explodedPosition,
  progress
)
```

实现：

```ts
position.lerpVectors(
  origin,
  exploded,
  progress
)
```

这样后续 Explosion Slider 无需重构 Explosion Engine。

---

# 27. Animation Manager

ExplosionManager：

> 计算目标位置。

AnimationManager：

> 负责如何从当前状态移动到目标状态。

建议：

```text
GSAP
```

示例：

```ts
gsap.to(animationState, {

  progress: 1,

  duration: 0.7,

  ease: 'power2.inOut',

  onUpdate() {

    updateTransforms(
      animationState.progress
    )
  }
})
```

---

# 28. 动画错峰策略

允许轻微 stagger：

```text
Module 1    0ms
Module 2   30ms
Module 3   60ms
Module 4   90ms
```

建议最大总错峰：

```text
≤ 150ms
```

确保用户仍感觉操作“即时”。

---

# 29. Camera Manager

统一封装：

```ts
focusProduct()

focusModules()

focusModule(moduleId)

resetCamera()
```

禁止业务组件散落：

```ts
camera.position.set(...)
```

CameraManager 同时控制：

```text
Camera Position
+
OrbitControls Target
```

---

# 30. Highlight Manager

职责：

```text
hoveredNodeId
     ↓
Object3D
     ↓
Highlight
```

可以使用：

- emissive；
- outline；
- material clone；
- postprocessing outline。

第一版优先考虑简单、稳定和高性能。

---

# 31. Label Manager

职责：

```text
Node
 ↓
Object Bounding Center
 ↓
World Position
 ↓
HTML / CSS Label
```

标签内容：

### Module

```text
Camera Module
```

### Part

```text
Camera Lens
```

---

# 32. UI 模块

建议：

```text
App

├── Header
├── Breadcrumb
├── Viewer
├── InfoPanel
├── ControlBar
├── LoadingOverlay
└── Labels
```

原则：

```text
UI
↓
dispatch Event
↓
State Machine
↓
Rendering Managers
```

UI 不直接操作 Object3D。

---

# 33. 核心数据流

以 Camera Module 为例：

```text
User Click Camera
        ↓
Pointer Event
        ↓
Raycaster
        ↓
Camera Mesh
        ↓
Object → Node Mapping
        ↓
camera-module
        ↓
State Machine
        ↓
MODULE → PART
        ↓
selectedModuleId = camera-module
        ↓
ExplosionManager
        ↓
计算 Parts Target Transform
        ↓
AnimationManager
        ↓
Tween Object3D
        ↓
CameraManager
        ↓
Focus Camera Module
        ↓
UI
        ↓
更新 Breadcrumb / Info
```

---

# 34. L1 → L2 完整执行链

```text
① User Click Product

② Raycasting

③ NodeResolver

④ 得到 Product Node

⑤ State Guard

   level === PRODUCT

⑥ isAnimating = true

⑦ ExplosionManager
   calculateModules()

⑧ AnimationManager
   progress 0 → 1

⑨ CameraManager
   focusModules()

⑩ 动画结束

⑪ level = MODULE

⑫ isAnimating = false

⑬ UI 更新
```

---

# 35. L2 → L3 完整执行链

```text
User Click Camera

↓

Raycasting

↓

Mesh

↓

NodeResolver

↓

camera-module

↓

State Guard

↓

selectedModuleId =
camera-module

↓

ExplosionManager

↓

Camera Parts
explode

↓

CameraManager
focusModule()

↓

level = PART

↓

Labels / Breadcrumb
更新
```

---

# 36. L3 → L2

执行：

```text
collapseModule(
  selectedModuleId
)
```

然后：

```text
selectedModuleId = null

level = MODULE
```

此时：

**其他 Modules 仍保持爆炸状态。**

---

# 37. L2 → L1

执行：

```text
collapseProduct()
```

所有 Module：

```text
explodedPosition
      ↓
assembledPosition
```

最终：

```text
level = PRODUCT
```

---

# 38. Zustand Store

建议：

```ts
interface ViewerStore {

  level:
    | 'PRODUCT'
    | 'MODULE'
    | 'PART'

  selectedModuleId:
    string | null

  selectedPartId:
    string | null

  hoveredNodeId:
    string | null

  isAnimating:
    boolean

  enterModules():
    void

  enterModule(
    moduleId: string
  ): void

  selectPart(
    partId: string
  ): void

  back():
    void

  reset():
    void
}
```

Store 只管理：

```text
ID
+
Application State
```

Three.js Runtime Object 存在 SceneRegistry。

---

# 39. 项目模块划分

建议划分为以下核心模块：

| 模块 | 核心职责 |
|---|---|
| Product Domain | Product / Module / Part 业务结构 |
| Model Loader | GLB 加载 |
| Scene Registry | Domain ↔ Object3D |
| Node Resolver | Mesh → Logical Node |
| Interaction Manager | Pointer / Raycasting |
| Viewer State | 当前交互状态 |
| Explosion Manager | 爆炸目标计算 |
| Animation Manager | Tween |
| Camera Manager | 相机聚焦 |
| Highlight Manager | Hover / Select 高亮 |
| Label Manager | 3D 标签 |
| UI | Breadcrumb / Panel / Controls |

---

# 40. 项目目录结构

```text
src/

├── app/
│   ├── App.tsx
│   └── routes/
│
├── viewer/
│   ├── ProductViewer.tsx
│   ├── ViewerCanvas.tsx
│   └── Scene.tsx
│
├── domain/
│   ├── product.types.ts
│   ├── product.config.ts
│   └── product.utils.ts
│
├── scene/
│   ├── ModelLoader.tsx
│   ├── SceneRegistry.ts
│   ├── SceneRegistrar.ts
│   ├── NodeResolver.ts
│   └── transform.utils.ts
│
├── interaction/
│   ├── RaycastManager.ts
│   ├── PointerManager.ts
│   └── HighlightManager.ts
│
├── explosion/
│   ├── explosion.types.ts
│   ├── ExplosionCalculator.ts
│   ├── ExplosionManager.ts
│   └── ExplosionAnimator.ts
│
├── camera/
│   ├── CameraManager.ts
│   └── CameraAnimator.ts
│
├── animation/
│   └── AnimationManager.ts
│
├── store/
│   └── viewer.store.ts
│
├── ui/
│   ├── Breadcrumb.tsx
│   ├── InfoPanel.tsx
│   ├── ControlBar.tsx
│   ├── LoadingOverlay.tsx
│   └── PartLabel.tsx
│
├── assets/
│   └── models/
│
└── styles/
```

---

# 41. 核心接口设计

## Scene

```ts
loadProduct()

getObjectByNodeId()

getNodeByObject()
```

---

## Navigation

```ts
enterModules()

enterModule(moduleId)

back()

reset()
```

---

## Explosion

```ts
explodeProduct()

explodeModule(moduleId)

collapseModule(moduleId)

collapseProduct()

setProgress(value)
```

---

## Camera

```ts
focusProduct()

focusModules()

focusModule(moduleId)

resetCamera()
```

---

## Interaction

```ts
hover(nodeId)

select(nodeId)

clearHover()
```

---

# 42. 性能优化方案

## 42.1 模型

优先考虑：

- GLB；
- Draco；
- Meshopt；
- KTX2；
- 减少高分辨率纹理；
- 减少重复 Geometry。

---

## 42.2 Raycasting

不要：

```text
scene.children 全量递归 Raycast
```

建议：

```text
interactiveObjects[]
```

只维护可拾取节点。

---

## 42.3 Renderer

建议：

```text
限制 DPR
```

例如：

```ts
dpr={[1, 2]}
```

移动端可以进一步降低。

---

## 42.4 Shadow

第一版避免大量实时阴影。

建议：

- 1 个主方向光；
- 环境光；
- 接触阴影或有限阴影。

---

## 42.5 Dispose

模型切换 / 卸载时需要正确释放：

- geometry；
- material；
- texture；
- event listeners；
- GSAP timeline。

---

# 43. 异常处理

需要考虑：

### 模型加载失败

```text
Retry
```

### GLB 节点不存在

输出：

```text
Scene registration failed:
Module_Camera not found
```

### 重复 Node ID

开发时直接抛错。

### Animation 被重复触发

State Guard 拦截。

### Raycast 命中不可交互对象

直接忽略。

### WebGL 不可用

给用户明确提示。

---

# 44. 开发阶段划分

考虑项目为短周期交付，建议按四个阶段推进。

## 阶段 1：基础工程与领域模型

完成：

- React；
- TypeScript；
- Vite；
- R3F；
- Scene；
- Camera；
- OrbitControls；
- product.types；
- product.config；
- GLB Loader。

产出：

**产品能够正常显示。**

---

## 阶段 2：Scene Mapping + Interaction

完成：

- SceneRegistry；
- SceneRegistrar；
- NodeResolver；
- Raycasting；
- Hover；
- Pointer / Orbit 冲突处理。

产出：

**能够稳定识别 Product / Module / Part。**

---

## 阶段 3：State Machine + Explosion

完成：

- Viewer Store；
- Level State；
- Interaction Guard；
- ExplosionCalculator；
- ExplosionManager；
- GSAP；
- Collapse；
- Camera Focus。

产出：

```text
PRODUCT
   ↓
MODULE
   ↓
PART
```

完整跑通。

这是项目最关键阶段。

---

## 阶段 4：UI + 性能 + 加分项

完成：

- Breadcrumb；
- Label；
- InfoPanel；
- Loading；
- Responsive；
- FPS 优化；
- BOM；
- Explosion Slider；
- README；
- 部署。

---

# 45. 开发优先级

严格按照：

```text
P0

三级 Domain Model
      ↓
Scene Mapping
      ↓
Raycasting
      ↓
State Machine
      ↓
Explosion
      ↓
Collapse
      ↓
Camera
      ↓
Hover
      ↓
Labels
```

然后：

```text
P1

UI
Loading
Responsive
Performance
```

最后：

```text
P2

BOM
Explosion Slider
Transparent Mode
```

不得先投入大量时间做：

- 粒子；
- Bloom；
- Shader；
- 复杂 UI 动效。

---

# 46. 项目主要风险

## 46.1 3D 模型结构不可控

风险：

模型节点可能：

```text
Cube001
Mesh019
Object37
```

导致无法可靠建立业务结构。

处理：

- 优先选层级清晰模型；
- 必要时 Blender 整理节点；
- 建立 objectName 配置层。

风险等级：

**高**

---

## 46.2 GLB 层级与业务层级不一致

风险：

一个 Module 可能由多个不连续 Mesh 构成。

处理：

引入：

```text
SceneRegistry
+
NodeResolver
```

禁止依赖单个 Mesh。

风险等级：

**高**

---

## 46.3 World / Local 坐标混用

风险：

爆炸位置错乱。

处理：

统一 Transform Utility，明确：

```text
World Space
Local Space
```

风险等级：

**高**

---

## 46.4 Click 与 OrbitControls 冲突

风险：

用户旋转模型时误触爆炸。

处理：

记录：

```text
pointerdown position

pointerup position
```

位移超过 threshold：

```text
Drag
```

否则：

```text
Click
```

风险等级：

**中**

---

## 46.5 动画过程中重复操作

风险：

Transform 状态错乱。

处理：

```text
isAnimating
+
Interaction Guard
```

风险等级：

**高**

---

## 46.6 模型过大

风险：

- 首屏加载慢；
- FPS 下降；
- 手机崩溃。

处理：

- Draco；
- Meshopt；
- Texture Compression；
- 模型简化；
- DPR 限制。

风险等级：

**中**

---

# 47. 验收标准

## 47.1 功能验收

### L1

- 产品完整显示；
- 能旋转；
- 能缩放；
- 能平移；
- Product 可 Hover；
- Product 可点击。

### L2

点击 Product：

- 所有 Modules 平滑展开；
- 不出现明显跳变；
- 每个 Module 可以独立识别；
- Module Hover 正常；
- Module Label 正常；
- Back 可回 L1。

### L3

点击任意 Module：

- 只有当前 Module Parts 展开；
- 其他 Modules 保持 L2 状态；
- Camera 自动聚焦；
- Part Label 正常；
- Back 返回 L2。

---

## 47.2 状态验收

必须满足：

```text
PRODUCT
   ↓
MODULE
   ↓
PART
```

并支持：

```text
PART
   ↓
MODULE
   ↓
PRODUCT
```

不存在非法：

```text
PRODUCT → PART
```

状态跳转。

---

## 47.3 动画验收

要求：

- 点击后立即开始动画；
- 爆炸平滑；
- 收合平滑；
- 重复进入 / 返回不发生位置漂移；
- 动画过程中快速点击不会破坏状态。

---

## 47.4 性能验收

桌面主流设备：

```text
FPS ≥ 50
```

同时：

- 无明显卡顿；
- 长时间操作性能不持续恶化；
- 模型加载过程有明确反馈。

---

## 47.5 工程验收

必须保证：

```text
UI 不直接操作 Mesh

Raycaster 不直接做动画

Store 不保存 Mesh

ExplosionManager 不管理 UI

GLB Object Name 不直接充当业务 ID
```

---

# 48. 交付物

最终交付：

### 1. 可访问 Demo

部署：

- Vercel；
- Netlify；
- GitHub Pages；
- 或其他静态托管服务。

### 2. 源代码

完整 Git Repository。

### 3. README

至少包括：

- 环境要求；
- 安装方法；
- 启动方法；
- Build 方法；
- 项目目录；
- 技术选型；
- 3D 模型来源。

### 4. 项目设计说明

包含：

- Product / Module / Part；
- Scene Graph；
- State Machine；
- Explosion Algorithm；
- Performance Optimization。

---

# 49. 项目成功标准

本项目成功不以“3D 模型足够精细”为唯一标准。

核心判断：

```text
业务结构是否清晰
        +
三级交互是否完整
        +
爆炸动画是否流畅
        +
场景架构是否合理
        +
工程实现是否可维护
```

最终必须达到：

> 用户无需阅读复杂说明，即可通过点击和三维操作理解“整机由哪些模组组成、模组又由哪些零件组成”。

---

# 50. 最终技术主链

```text
                         Product Config
                               │
                               ▼
                         Domain Model
                               │
                               ▼
GLB Model ────────────→ SceneRegistry
                               │
                       Object ↔ NodeId
                               │
                               ▼
                           Raycasting
                               │
                               ▼
                          NodeResolver
                               │
                               ▼
                         Viewer State
                         State Machine
                               │
                ┌──────────────┼──────────────┐
                ▼              ▼              ▼
          ExplosionManager CameraManager HighlightManager
                │              │              │
                └──────────────┼──────────────┘
                               ▼
                         Three.js Scene
                               │
                               ▼
                           React UI
```

项目的核心架构可以最终浓缩为：

```text
Domain Model
+
Scene Graph Mapping
+
State Machine
+
Explosion Engine
+
Interaction System
+
Camera System
```

---

# 51. 开发约束

后续代码开发严格遵循以下原则：

1. **Product Structure 与 Three.js Scene Graph 分离。**
2. **业务状态与 Runtime 3D 对象分离。**
3. **所有 Three.js Object 统一由 SceneRegistry 管理。**
4. **用户交互必须先经过 NodeResolver 与 State Guard。**
5. **所有爆炸动画必须可逆。**
6. **初始 Transform 不允许被覆盖。**
7. **Explosion Progress 从第一版保留。**
8. **相机操作统一由 CameraManager 执行。**
9. **UI 只 dispatch command，不直接改 3D Object。**
10. **优先保证三级交互和动画，再开发加分项。**

---

# 52. 立项结论

本项目方案已经完成需求收敛和核心技术架构设计，当前不存在必须阻塞开发的重大需求缺口。

正式开发建议从以下文件开始：

```text
product.types.ts
      ↓
product.config.ts
      ↓
SceneRegistry.ts
      ↓
SceneRegistrar.ts
      ↓
NodeResolver.ts
      ↓
viewer.store.ts
      ↓
ExplosionCalculator.ts
      ↓
ExplosionManager.ts
```

完成上述基础设施后，再进入：

```text
Interaction
Camera
Animation
UI
```

最终形成一个具备清晰业务模型、稳定状态控制和良好三维交互体验的 Web 3D 产品爆炸图系统。

---

**项目状态：批准立项，可进入工程编码阶段。**
